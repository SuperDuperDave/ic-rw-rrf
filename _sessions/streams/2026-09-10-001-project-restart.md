# Project restart and research orientation — 2026-09-10

## Resume

- Objective: adapt Ascend's session workflow, establish project-scoped Agent
  Relay if usable, and recover the actual research frontier and next steps.
- Now: restart complete; workflow installed, research state audited, bounded
  authenticated Claude review through Relay verified and incorporated.
- Next: PLANNING R1 (canonical evaluation contract), then R2 (trained k on
  untouched evaluation); C1 holds the prepared website case-study handoff.
- In flight: none. Claude ended; no pending Relay signals or active claims.
- Preserve: the May 13 basin-escape stream and five untracked evaluation probes
  were present before this task. Do not overwrite or silently adopt their claims.
- Starting revision: `3d00ed3`; no tracked changes at entry.

## Scope and decisions

- Dave wants the project to demonstrate exploratory research and how he thinks,
  including useful negative results. A breakthrough is not an acceptance gate.
- Ascend and Agent Relay have other active sessions. Inspect their integration
  read-only; keep this project's enrollment/runtime isolated.
- Shared AGENTS + Claude import, map, backlog, workflow, friction register, and
  portable session helper replace reliance on personal sibling skills.

## Evidence and outcomes

- README and old CLAUDE stop at v6/v7. The untracked second May stream contains
  MC4, score, empirical-decay, aggregation-operator, and provisional k-tuning
  experiments. Its cross-ensemble gain was not statistically significant.
- Initial code inspection found the provisional k-tuning probe starts ranks at
  zero while the common harness starts at one. Historical baselines differ;
  harmonization precedes any promotion of v8.
- Official Codex AGENTS documentation retrieved; local Ascend active-worktree
  initialize/session conventions inspected. No changes to either sibling repo.

## Hypotheses and opportunities

- A research case study tracing predictions, failures, corrections, and the
  next discriminating experiment fits the intended portfolio well.
- Broader ranker/data diversity and a clean evaluation protocol may be more
  informative than another variation on a repeatedly selected lexical benchmark.

## Friction & noise log

- Broad Ascend searches entered historical worktrees and flooded output.
  PROMOTE: pin the active source and read targeted role sections.
- The first subagent spawn was rejected for implicitly copying full history.
  SUBTRACT: give bounded workers explicit context with `fork_turns=none`.
- Current research state is split between stale public docs and untracked notes.
  PROMOTE: RESEARCH_STATE owns the current synthesis; MAP routes to evidence.
- Managed `.agents`/`.codex` are read-only here. SUBTRACT: ordinary repo files
  provide the workflow; avoid requiring global or managed-directory changes.

## Friction wrap-pass

- SUBTRACT: shared AGENTS/Claude entry, portable status/start helper, local
  initialize shim replacing a symlink that had drifted into TWE Staffing.
  The shared mainthread source was never edited.
- PROMOTE: current state in RESEARCH_STATE, body-once backlog, research
  comparison contract in R1, scoped Relay helper and immutable artifact formats.
- DROP: no new permanent permission ritual from the one external-egress
  auto-review rejection; Dave explicitly approved the bounded Claude review.
- FRICTION F1–F8 retain precise symptoms, landed fixes, and reuse evidence.
  Session-to-session QoL improvements remain awaiting actual later reuse.

## Completion evidence and decisions

- New shared agreement, Claude import + local initialize skill, workflow, MAP,
  PLANNING, RESEARCH_STATE, FRICTION, and helper scripts are in place.
- Reproduced synthetic demo (50 queries) and full v6 REF validation panel.
  Raw REF output and 21 input/probe hashes are in `_sessions/evidence/`.
- All 13 helper tests passed; new helpers parse under Python 3.8 grammar.
  Initialize skill validator passed; local document links and diff checked.
- Corrected README's universal-superiority, parameter-count, rank-delta,
  label-threshold, and comparator language. Historical specs/results/notebooks
  and all five pre-existing untracked probes remain unchanged.
- Prepared `docs/RESEARCH_BRIEF.md` for the separate website effort. Nothing
  published or sent to that other task; all source changes remain uncommitted.
- Installed Relay enrollment and SQLite doctor succeeded in normal host context.
  Sandbox ancestry refusal was environment-specific. No sibling runtime/ledger,
  provider settings, or global hooks changed. Only this project's required
  enrollment record was created in Relay's account enrollment store.
- A real Claude review, approved by Dave after automatic review initially
  rejected egress, returned the context-only marker and usable findings.
  Exit 0, `is_error=false`, matching lifecycle events; no provider remained alive.
  Coordinator recorded exact ACK on Claude's request after reading full event
  and verifying review. Final brief empty. This is not proof of automatic
  wake/resume or a full autonomous coding workflow.
- Adopted adverse-collection disclosure, recurrence of configuration-specific
  wins, explicit reproduction evidence, and query-cluster inference planning.
  Corrected Claude's mistaken offset sign rather than accepting it: canonical
  k = local k − 1 (apart from weighting/ties). Research implementation untouched.

## Carryforward

- [R1](../PLANNING.md#r1--next-establish-one-comparable-reproducible-evaluation-contract): repair comparison contract.
- [R2](../PLANNING.md#r2--next-after-r1-test-whether-k-tuning-survives-honest-selection): test trained k under honest selection.
- [C1](../PLANNING.md#c1--next-turn-the-research-brief-into-the-website-case-study): website case-study handoff.
- [O2](../PLANNING.md#o2--parked-broader-relay-automation-after-its-provider-integration-settles): broader Relay automation awaits its own development evidence.
