# Cycle13 design decision

**RUN one fixed local audit**, under the
[protocol](2026-09-11-cycle13-judgment-protocol.md). It returns to the original
retrieval question through the value of another judgment. This is distinct from
cycle03's stopped specialist association and cycle12's closed public-byte case.
No new labels or empirical model responses are collected.

An independent Codex mathematical lens confirms the shared-document interval,
attainable endpoints and equal-cost width rule. It flagged the distinction between
per-query and global annual budgets; the protocol now explicitly fixes per-query
budgets. No claim of optimal annual allocation follows. Neither reviewer ran
the data. Implementation and synthetic tests precede the actual audit.

## Claude's review and qualifications

Fable5.1 reviewed the unpublished direction with current project context under
Dave's expanded sharing authorization. It recommended RUN. The complete
[public memo](2026-09-11-cycle13-claude-design.md) is preserved, including its
assumptions and proposals. It had no tools, scouts or independent source access.

Adopt the width/sign distinction, current per-year bounds and explicit inclusion
of already-determined/zero-width cases. Annual intervals are **means**, not sums,
so their units match the declared metric. Add selected-set overlap as a descriptive
comparison with pooled-head judging; retain interval width as the prespecified
primary observable. Uniform is a reference expectation, not a universal floor:
pooled-head ordering can be better or worse than it.

Do not adopt the review's assertion that pooled labels necessarily bias this
comparison against B, or that B's novel entries are more often unjudged. These
were not measured. Treating missing gains as zero sets the unknown contributions
to zero; it can favor either ranking depending on their signed coefficients.
Publication timing alone cannot establish the missing-label distribution.
Same-set selections at a budget establish equal choices there, not redundancy of
all disagreement-based acquisition methods.

The proposed condition `residual_width < max(U,-L)` for possible strict sign
resolution is compatible with the independent box model when the original
interval includes zero. It describes favorable possible label outcomes, not a
guarantee. A new minimum-budget sweep is unnecessary for this first evidence map
and is not added. Current certificates and projected widths remain separate.

Likewise, the suggested SPLADE-only selection breakdown is not an automatic
second analysis. Even a concentration there would describe these selections;
it would not prove a rule reduces generally to judging novel-source documents.
Stop after the frozen pair's evidence map, then decide whether further work
has a distinct purpose.

## Collaboration receipt

One Fable5.1/high invocation succeeded in78.53954seconds at$0.4884975 native list
accounting, below fixed$1.50/180second bounds. Output4761 includes3621 thinking
tokens across two observed message IDs; final per-message telemetry is unavailable.
The4000 setting is per API response. Its521-word memo exceeded500 advisory.
Terminal output held only the second fragment; complete assistant-text extraction
recovered both without another call. No thought text is published.

Request70 and full memo were read; lifecycle71–73 completed and Codex recorded
Claude's requested consumptionACK74. Expanded consent was accepted for the actual
unpublished payload, with no repeated permission request. The
[receipt](../evidence/2026-09-11-cycle13-design-receipt.json) separates scope,
native accounting and delivery. No model-tier efficiency or scientific benefit
is inferred from a successful review.
