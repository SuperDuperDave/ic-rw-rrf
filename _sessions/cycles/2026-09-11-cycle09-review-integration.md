# Cycle09 review integration

The [original Fable memo](2026-09-11-cycle09-claude-review.md) recommends RUN.
The coordinator accepts exactly two prospective calls after a separate execution
freeze: native semantic/format feasibility is still unobserved. The local gate
already passes; no new seed, order, corruption, prompt search or reserve use.

The actual all-zero program and V-first order make the result particularly
limited. First-original-only scores 3/3 in both packets, so perfect judgments
would establish observed output correctness on this case, not a checking strategy
or general instruction-following ability. Exact machine checking remains the
practical baseline. A native failure is a collection failure; a well-formed
incorrect map is a mathematical judgment error.

Corrections to the supplied review:

- A changed response across fresh contexts does not establish that copies caused
  it. The same certificates appear inside different overall evidence contexts;
  added text, F-only duplication, fixed order and stochastic outputs all remain.
  Any original can change, not only V or I. No change alone is a failure if the
  change corrects an initially wrong judgment.
- A format null does not establish its cause. The 500-token bound is per API
  response including thinking; native continuations can exceed that amount per
  invocation. Record actual stop/usage metadata without presuming cap exhaustion.
- The execution protocol was pending when the review ran, not already sealed.
  Local construction was sealed. Both advertised hashes have different intended
  objects: Relay identifies the mission; the capsule identifies the fixture's
  manifest. The capsule receipt verifies both, so inequality is expected.
- A future nonzero/reordered/I-copy test could distinguish specified output
  policies better. It still would not uniquely identify internal reasoning or a
  causal repetition effect. Do not choose a replacement draw for cycle09 or use
  visual inconspicuousness as an unrecorded selection rule.

The complete public-text memo was read; private thinking stays ignored. Request50
and lifecycle51–53 identify the review. Claude requested consumption ACK50;
Codex recorded it as event54 after reading the exact request and memo. Final
brief is clear. [Native receipt](../evidence/2026-09-11-cycle09-review-receipt.json).
