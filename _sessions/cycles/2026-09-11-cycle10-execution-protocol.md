# Cycle10 — fixed six-call response-policy diagnostic

This contract becomes executable only after a passed local audit and a separate
matching decision receipt. It does not change the previously committed
[construction](2026-09-11-cycle10-discriminating-design.md) or
[local supplement](2026-09-11-cycle10-local-protocol.md).

**Question and prediction.** Do actual returned validity judgments agree with
exact verification across the fixed position/endpoint panel, and thereby differ
from the named reference vectors that also could explain cycle09's success?
Successful judgment predicts18 correct decisions. Incorrect well-formed maps
are scientific observations. Native or format failures leave the full primary
unavailable, with their actual stop causes recorded separately.

**Fixed data and references.** Two programs, A=2/B=3, R0 and R1. Each has V/I/F
certificates and three cyclic orders. Six packets each contain three originals
and two null slots; no copies are added. There are six certificates from two
endpoint variants of one arithmetic skeleton, judged18 times. V/I final a=7/9
share parity; I's row3 error retains the true endpoint. V validity is true and
I/F false. Endpoint values and positions are balanced; validity counts are not.

Exact checking scores18/18; always-valid6/18; always-invalid12/18; each policy
accepting only one fixed position10/18; endpoint-agreement12/18; raw endpoint
Boolean9/18. Preserve all predictions and vector mismatches, not only scores.
The local checker must independently reconstruct these counts before execution.

**One fixed workflow.** Exactly (R0,P1), (R1,P2), (R0,P3), (R1,P1), (R0,P2),
(R1,P3). Each uses a fresh UUID and empty working directory. Reuse cycle09's
exact system instruction and strict original-ID Boolean parser unchanged;
the corresponding sealed canonical packet is the entire user input. No private
V/I/F labels, reference maps, filenames, worked examples or previous responses
enter provider inputs. The execution manifest pins source/test/protocol bytes,
the common system prompt and all six payloads before any response.

Use `claude-opus-5`, high effort, pinned native2.1.267 with SHA256
`0399c793ff571d5946ef923d80b4f330d05ac4b6842a6b0775468f5d389403c0`.
Reuse the inspected command/environment contract with no tools, hooks, MCP,
agents, project memory, browser, persistence, slash commands, workflow dispatch,
fast mode or alternate-model fallback. Check inherited overrides and binary
identity. Native metadata/model/activity/session/cost checks gate acceptance.

**Resources and stopping.** At most six serial native invocations, $1 remaining
native scheduling allowance,120seconds per invocation,300seconds per batch,
max-turns1,500 output tokens per API response including thinking. Continuations
may exceed500 aggregate tokens per invocation; wire attempts are incompletely
observable. Stop at the first native, format, custody or resource failure. No
retry, repair, reserve, replacement, fallback, prompt/model search or subsequent
wave. Wrong well-formed maps do not suppress the remaining planned calls.

**Primary and partial reporting.** Parse only the unique terminal result:
exactly three original IDs with strict Boolean values; no duplicate/extra/missing
keys, numeric substitutes, prose or partial salvage. Full primary requires all
six maps and records correct/accepted/planned counts plus disagreements with
each complete named policy vector. Always retain all18 planned rows; partial
comparisons report their actual denominators with full-vector equality null.
Record each root's decisions across positions and each type across R0/R1.
Invalid/unsent responses are never imputed as incorrect mathematical judgments.

**Interpretation checkpoint.** A perfect vector excludes only these named
deterministic response vectors on this fixture. It does not identify internal
reasoning, exclude other shortcuts, isolate causal position/endpoint effects,
or establish generalization, independent samples, novelty or multiagent benefit.
If perfect, stop this diagnostic and consider a consequential evidence-selection
decision. If imperfect, preserve its counterexample; matching a heuristic vector
does not prove use of that heuristic. Machine execution remains the practical
verifier. Do not respond to either outcome by searching for harder arithmetic.

Score offline, independently audit raw/custody/reference reconstruction, then
seek one bounded Fable interpretation review through Relay. Empirical contexts
remain separate from review context. Review accounting is reported separately;
its own $1/180second bound does not enlarge this batch's limits. No follow-up
empirical work is part of this protocol.

```bash
python3 _sessions/tools/run_cycle10_verifier.py prepare --prepared-manifest-sha256 <local-sha>
python3 _sessions/tools/run_cycle10_verifier.py collect --execution-manifest-sha256 <execution-sha>
python3 _sessions/tools/score_cycle10_verifier.py --score
```

Directories are exclusive. Exact manifest identities go in the decision, native
records and final report; never overwrite frozen artifacts. Raw provider streams
stay ignored. Native list accounting is not subscription billing.
