# Cycle06 execution contract

Frozen before any cycle06 provider response. The unchanged
[design](2026-09-10-cycle06-known-noise-design.md), SHA256
`2c1c0f9e5d79236a2443d24c907f2da623238793bb19df2b3bac27aa1b495095`,
owns the hypothesis, channel, eight inputs, reference predictions and stopping
rule. No parameter, prompt, model or outcome threshold is selected by responses.

## Observation and exact arithmetic

Retain the cycle04 truth, conditional sensor independence, known calibration and
equal construction priors. On non-null copied/independent reports, the binary
hint has probability 3/4 of naming the true construction, independent of truth
and primitive readings given that construction. Supply the full model and this
channel, fixed opaque slot IDs, four report values and the **noisy hint**. No
actual construction, true partition, latent reading, answer, acquired count or
evaluation identifier enters the submitted prompt. Construction names describe
the supplied model and observed hint; they do not disclose realized truth.

Exactly eight unique observations: two specialist reliabilities × two signs ×
two hints, with unanimous generalists opposing the specialist. The preparation
records exact serialized system/user bytes and hashes, seed42 order, posterior
and comparator probabilities, masses and weights before collection. References
are independently reconstructed from the immutable cycle04 world table and
closed-form likelihoods. Zero/half channel error and padding stay analytic.

Primary is conditional excess Brier against the correct noise-aware posterior,
separately by specialist reliability, using augmented-generator diagnostic
weights. Compare direct expected Brier against exact blind and naive hint trust
on the same worlds. Track individual errors/decisions, hint contrasts and sign
symmetry. Full primary requires all eight valid responses. Otherwise preserve
coverage and descriptive valid-subset scores with explicit changed masses/counts;
do not impute or infer independent sample uncertainty from the selected packets.

## Reused native controls and provenance

Native executable `/home/david-wsl/.local/share/claude/versions/2.1.267`, SHA256
`0399c793ff571d5946ef923d80b4f330d05ac4b6842a6b0775468f5d389403c0`, was
rechecked at this continuation. No inherited provider/Claude override keys were
present. The native byte identity matches the prior
[control audit](2026-09-10-cycle05-native-controls.md); its limitations still
apply. Model ID is `claude-opus-5`, with explicit high effort. This pins a client
and model identifier, not immutable provider weights.

Reuse the preserved cycle05 process driver and corrected zero-agent-counter
parser through a scoped cycle06 adapter. Original source files stay unchanged.
Only the prepared fixture/payload validator, output directory and prospective
batch limits change. The collector's private storage convention remains
`_sessions/local/cycle05/<unique-batch-id>`; each durable cycle06 receipt records
the exact ignored stream path and hash. No prior response enters this batch.

Fresh empty temporary cwd and session UUID per invocation; exact frozen user
bytes enter stdin, with the fixed system prompt. Safe mode, empty setting sources,
disabled hooks/auto-memory, empty tools/MCP, no browser/slash commands, no session
persistence and the existing environment controls remain in force. Experimental
inputs bypass Relay hook context; scientific collaboration is a separate phase.
Ordinary repository context is suppressed; native/provider overhead and managed
policy caveats remain. Do not read or change credentials or provider settings.

The start manifest anchors preparation, new adapter/tests/scorer, every reused
implementation, protocols, exact argv/environment and native binary. Verify
source/output identities before and after collection. An immutable directory
and pre-invocation attempt receipts preserve incomplete/error outcomes. A custody
failure invalidates the collection and prevents presentation as the frozen run.

## Resource and acceptance boundaries

- At most **eight native invocations**, sequentially. No application retries,
  replacements, fallback, resumed contexts, prompt edits or automatic repair run.
- 500 output tokens **per API response**, including reasoning; native same-turn
  continuation/recovery remains part of the fixed workflow. Observe distinct
  message IDs, cumulative usage and recovery markers without claiming a complete
  wire-attempt count or 500 total tokens per invocation.
- 120 seconds per invocation and 300 seconds total batch wall time. Each call
  gets the smaller remaining deadline. Terminate/reap only the owned process
  group on timeout and stop the batch.
- $1 cumulative native-accounting scheduling allowance. Each invocation gets
  the remainder as its native cap. Stop scheduling at exhaustion; a single-call
  overshoot is possible. This is not a hard billing or subscription-credit bound.
- Require native exit0/success, a unique terminal result ending `end_turn`, known
  finite nonnegative cost, the intended canonical model, empty tools/MCP and no
  actual tool/hook/subagent activity. Recognize only the verified exact all-zero
  integer subagent-statistics schema. Observed per-message output above500 fails
  the configuration gate; missing usage remains unverified rather than zero.
- Parse only terminal `result.result` as one JSON object with the sole finite
  numeric `p_positive` in [0,1]. Reject duplicate/extra keys, booleans, strings,
  prose/fences and nonfinite values. Never stitch or salvage earlier fragments.
  An invalid answer with otherwise acceptable native execution may continue the
  frozen order. Native/configuration errors, unknown cost or timeout stop it.

Stop after one batch, offline scoring and independent raw/arithmetic review.
Both a valid-subset result and a complete calculation have explicit interpretation
limits. Any separate Claude interpretation review gets its own scope and spend
receipt and cannot supply or replace experimental predictions.
