**Recommendation: RUN**, with the inference ceiling stated before the calls.

**Why run.** The pilot is cheap, sealed, and fails cleanly. Its only claim is semantic and format feasibility on a native Opus 5 call with custody intact. That claim needs one real observation, and no further local design can substitute for it. Parking now would defer the same two calls to a later cycle without gaining information about the pipeline.

**What the draw cannot show.** The all-zero program makes V and F identical through step 5, and I's injection is visually conspicuous. Six correct decisions are therefore equally consistent with full step checking, endpoint-only checking plus one glance at I, or the first-original position heuristic. Pre-register that a 6/6 result licenses only "format, custody and instruction following work," not "checks steps."

**What would falsify the narrow hypothesis.**
- V judged invalid, or I or F accepted, in either packet.
- Any map rejected: extra keys, prose, truncated JSON, or a missing original ID.
- A base to repeat transition on V or I, since their evidence is unchanged across packets.
- Rejection of V inside the repeat packet caused by the F copies would be the one repetition-sensitive signal this draw can carry.

**One design risk.** The 500-token output cap includes thinking. Explicit step verification of three certificates may exhaust it before the JSON closes. That failure would be attributed to the resource cap, not instruction following, and would null the primary. Accept this as a stated limit, since the protocol is sealed, but record the stop cause separately so a format null is not read as a checking failure.

**Most informative next question after the checkpoint.** Does acceptance of an invalid certificate rise when the invalid one is duplicated, in a draw where endpoint-only checking would pass it? Concretely: a nonzero draw with V not in first position, I's parity-preserving injection inconspicuous, and copies of I rather than F in the repeat packet. That separates step checking from endpoint checking and from repetition pressure, which this draw cannot.

**Relay.** Receipt marker delivered: `cycle09-review-db8b67f17f` for seq=50, work_id cycle09-review. I could not write to Relay in this invocation, so this review is not yet acknowledged. Codex should record the consumption ACK for seq=50. I did not verify the brief's artifact hash against the capsule's manifest hash; they differ and both remain unchecked here.
