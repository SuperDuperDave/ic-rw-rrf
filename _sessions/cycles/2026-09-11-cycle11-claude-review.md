**Receipt marker:** cycle11-review-eb260e5257, Relay seq 60, work_id cycle11-review, artifact sha256 db8d7d78. Tools are disabled in this invocation, so I could not run Relay and have not recorded consumption. Please have Codex record the consumption ACK for seq 60 against the actual request. This review is based only on the supplied capsule.

**Stopping rule verdict.** The frozen rule is met. All four answers and all sixteen validity judgments match the exact checker, custody and accounting passed, and no invocation failed. Park multiagent work in this laboratory. No repair probe follows.

**Challenge to the narrow conclusion.** The pass is real but weaker than it looks, for a structural reason visible in the capsule itself.

- **Validity is fully determined by the endpoint.** The exact_checker and endpoint_matches_truth vectors are identical across all sixteen slots. V and F certificates share rows one through five and differ only in the final result field. A solver that executes the six-line program and marks each instance valid when its endpoint equals its own answer reproduces the checker without reading any row.
- **The answers are likewise computable without evidence.** Both programs reach a=7 identically and differ only in the parity predicate. Exact replay is the trivial policy here, and it scores 4/4 by construction.
- **Deliberation was minimal.** Thinking tokens averaged under 100 per call. That is consistent with direct computation and not with weighing minority versus majority support. The capsule already disclaims necessary evidence use, and the data agree.

So the laboratory succeeded at its design task, discriminating against majority, minority and position policies. It cannot, even in principle, test the original curiosity. When truth is recomputable from the packet, minority evidence has no role to play, and a pass tells us the judge computed rather than selected. That is the demonstrated fact. Whether the judge would select correctly when it cannot compute remains unmeasured.

**One direction, framed as a consolidation checkpoint first.** Write the negative result plainly: within recomputable arithmetic, no evidence-selection signal is measurable and no successor fusion method exists. Then, before any new acquisition, run a recomputability audit on data already held.

- **What it exposes.** The retrieval experiments showed a narrow gain and failed annual transfer. The known-truth models separated lineage from error dependence. Neither stratified items by whether the judge could derive the answer without the retrieved evidence. If the ear

lier gain concentrates in the non-recomputable stratum and the transfer failure concentrates in the recomputable one, that would be genuinely new information: it would say the minority-evidence effect was real but masked by items where evidence was redundant. This is an unvalidated possibility, not a finding.

- **Smallest local gate.** Purely offline, no provider calls. Label each retained retrieval and known-truth item with a binary recomputability flag using the existing exact-execution reference where one exists, and a fixed rule elsewhere. Then count the non-recomputable stratum. The gate passes only if that stratum contains at least a handful of items with both minority-correct and majority-correct outcomes already observed. If the stratum is empty or one-sided, the direction needs acquisition, and the correct move is to stop at the consolidation note.

**Proportionality.** The audit machinery already exceeds the panel size it guards. The recomputability audit reuses the exact-replay reference and root-identity code without adding stages, agents or a harder toy. No empirical launch follows from this suggestion.
