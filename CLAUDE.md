# IC(R/W)-RRF — Claude entry point

@AGENTS.md

Read and follow the shared `AGENTS.md` agreement before work. Initialize through
`_sessions/WORKFLOW.md`, `_sessions/MAP.md`, `_sessions/PLANNING.md`, and
`_sessions/RESEARCH_STATE.md`; continue the current task stream on resume.

The project explores adaptive rank fusion and values both supported improvements
and useful negative results. The current frontier and claim qualifications live
in RESEARCH_STATE, not in a duplicated version summary here.

For Relay collaboration, read `_sessions/RELAY.md`. An ordinary delegated reviewer
returns findings to its coordinator; it does not start a second project journal.
Historical `.claude/skills/` links remain optional; startup does not depend on them.
